<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>วิเคราะห์ตัวตนแบบลึกซึ้ง</title>
  <style>
    body {
      font-family: 'Sarabun', sans-serif;
      max-width: 700px;
      margin: auto;
      padding: 20px;
      background-color: #f4f4f4;
      color: #333;
    }
    h2 {
      color: #2e7d32;
    }
    input, textarea, button {
      width: 100%;
      padding: 10px;
      margin: 10px 0 20px;
      font-size: 16px;
    }
    button {
      background-color: #388e3c;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background-color: #2e7d32;
    }
  </style>
</head>
<body>
  <h2>📋 คำสั่งหลัก: วิเคราะห์ตัวตนลึกซึ้งแบบองค์รวม</h2>
  <p>กรอกข้อมูลของคุณเพื่อให้ AI วิเคราะห์ตัวตนและดวงชะตาอย่างละเอียด</p>

  <label>ชื่อ-นามสกุล (ภาษาอังกฤษ):</label>
  <input type="text" id="fullname" placeholder="เช่น: Plernphun Seniwongse">

  <label>วันเดือนปีเกิด (เช่น 28 สิงหาคม 2528):</label>
  <input type="text" id="birthdate">

  <label>เวลาเกิด (เช่น 16:45):</label>
  <input type="text" id="birthtime">

  <label>สถานที่เกิด (จังหวัด/ประเทศ):</label>
  <input type="text" id="birthplace">

  <label>ข้อความเพิ่มเติมหรือเป้าหมายชีวิตที่อยากให้ AI ช่วย:</label>
  <textarea id="goal" rows="4" placeholder="เช่น: อยากค้นหาจุดแข็ง จุดอ่อน เป้าหมายชีวิต ความสัมพันธ์ หรืออาชีพที่เหมาะกับตัวเอง"></textarea>

  <button onclick="generatePrompt()">🔍 วิเคราะห์กับ AI</button>

  <script>
    function generatePrompt() {
      const name = document.getElementById("fullname").value;
      const birthdate = document.getElementById("birthdate").value;
      const birthtime = document.getElementById("birthtime").value;
      const birthplace = document.getElementById("birthplace").value;
      const goal = document.getElementById("goal").value;

      const prompt = `
คุณคือผู้เชี่ยวชาญระดับโลกด้านการวิเคราะห์ชีวิตและการค้นหาตัวตนที่แท้จริง ที่มีความเชี่ยวชาญใน:
- จิตวิทยาลึก (Depth Psychology)
- เลขศาสตร์และดวงชีวิต (Numerology & Life Analysis)
- ปรัชญาตะวันออกและตะวันตก
- จิตวิทยาบวกและการพัฒนาตนเอง
- การวิเคราะห์บุคลิกภาพและจุดแข็ง-จุดอ่อน
- ศาสตร์แห่งความสมดุลชีวิต

หน้าที่ของคุณคือ:
1. วิเคราะห์ข้อมูลส่วนตัวของลูกค้าอย่างละเอียดและลึกซึ้ง
2. ให้คำแนะนำที่เป็นประโยชน์จริงและนำไปปฏิบัติได้
3. ช่วยลูกค้าเข้าใจตัวเองในระดับที่ลึกที่สุด
4. แนะนำวิธีการสร้างความสมดุลและเป็นตัวเองอย่างแท้จริง

ข้อมูลของฉัน:
- ชื่อ: ${name}
- วันเกิด: ${birthdate}
- เวลาเกิด: ${birthtime}
- สถานที่เกิด: ${birthplace}
- เป้าหมายหรือความต้องการพิเศษ: ${goal}

กรุณาวิเคราะห์ตามหลักของคุณ โดยใช้ภาษาที่อ่อนโยน ตรงไปตรงมา สร้างแรงบันดาลใจ พร้อมแนวทางที่นำไปใช้ได้จริง
      `;

      const encodedPrompt = encodeURIComponent(prompt);
      const finalLink = `https://chat.openai.com/?prompt=${encodedPrompt}`;
      window.open(finalLink, "_blank");
    }
  </script>
</body>
</html>


<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>แบบฟอร์มวิเคราะห์ตัวตน</title>
  <style>
    body {
      font-family: 'Sarabun', sans-serif;
      max-width: 700px;
      margin: auto;
      padding: 20px;
      background-color: #f9f9f9;
      color: #333;
    }
    h2 {
      color: #4a148c;
    }
    select, textarea, input, button {
      width: 100%;
      padding: 10px;
      margin: 10px 0 20px;
      font-size: 16px;
    }
    button {
      background-color: #4CAF50;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background-color: #388E3C;
    }
    .hidden {
      display: none;
    }
  </style>
</head>
<body>
  <h2>🔮 วิเคราะห์ตัวตนในแบบของคุณ</h2>
  <p>เลือกหมวดที่คุณสนใจ กรอกข้อมูล แล้วเริ่มต้นวิเคราะห์กับ AI</p>

  <label>เลือกหมวด:</label>
  <select id="category" onchange="updatePrompt()">
    <option value="">-- เลือกหัวข้อ --</option>
    <option value="life">1. วิเคราะห์เลขศาสตร์และดวงชีวิต</option>
    <option value="personality">2. วิเคราะห์บุคลิกภาพเชิงลึก</option>
    <option value="purpose">3. ค้นหาจุดมุ่งหมายชีวิต</option>
    <option value="relationship">4. ความสัมพันธ์และพลังงาน</option>
    <option value="career">5. อาชีพและการเงิน</option>
    <option value="bonus">🎁 วิเคราะห์สุขภาพและจิตวิญญาณ</option>
  </select>

  <div id="dynamicFields">
    <label>ชื่อ-นามสกุล (ภาษาอังกฤษ):</label>
    <input type="text" id="fullname" placeholder="เช่น Plernphun Seniwongse">

    <label>วันเกิด (เช่น 28 สิงหาคม 2528):</label>
    <input type="text" id="birthdate">

    <label>เวลาเกิด:</label>
    <input type="text" id="birthtime">

    <label>สถานที่เกิด:</label>
    <input type="text" id="birthplace">

    <label>ตอบคำถามเพิ่มเติม (ตามหัวข้อที่เลือก):</label>
    <textarea id="customAnswers" rows="5" placeholder="ใส่คำตอบหรือข้อมูลที่เกี่ยวข้อง เช่น คำตอบจากคำถามเชิงลึก"></textarea>
  </div>

  <button onclick="generatePrompt()">✨ เริ่มวิเคราะห์กับ AI</button>

  <script>
    function updatePrompt() {
      document.getElementById("customAnswers").value = "";
    }

    function generatePrompt() {
      const category = document.getElementById("category").value;
      const name = document.getElementById("fullname").value;
      const birthdate = document.getElementById("birthdate").value;
      const birthtime = document.getElementById("birthtime").value;
      const birthplace = document.getElementById("birthplace").value;
      const custom = document.getElementById("customAnswers").value;

      if (!category) {
        alert("กรุณาเลือกหมวดการวิเคราะห์ก่อนค่ะ");
        return;
      }

      let prompt = "";

      if (category === "life") {
        prompt = `
คุณคือผู้เชี่ยวชาญด้านเลขศาสตร์และดวงชีวิตระดับสากล มีประสบการณ์มากกว่า 20 ปี
ข้อมูลของฉัน:
- ชื่อ: ${name}
- วันเกิด: ${birthdate}
- เวลาเกิด: ${birthtime}
- สถานที่เกิด: ${birthplace}

กรุณาวิเคราะห์:
1. เลขประจำตัว (Life Path Number) และความหมาย
2. จุดแข็งเด่นชัดที่สุด 5 อันดับแรก
3. จุดอ่อนที่ต้องระวังและวิธีแก้
4. โอกาสทางการเงินและอาชีพ
5. ความสัมพันธ์
6. ช่วงเวลาสำคัญในชีวิต
7. คำแนะนำเฉพาะสำหรับฉัน
        `;
      } else if (category === "personality") {
        prompt = `
คุณคือนักจิตวิทยาเชิงลึกที่ผสมผสานความรู้จาก Jung, Maslow และปรัชญาตะวันออก
ข้อมูลเบื้องต้น:
- ชื่อ: ${name}
- วันเกิด: ${birthdate}
- คำตอบลูกค้า:
${custom}

กรุณาวิเคราะห์:
- รูปแบบบุคลิกภาพหลัก
- แรงจูงใจเบื้องหลัง
- พฤติกรรมที่เกิดซ้ำ
- จุดแข็งที่ยังไม่ถูกใช้
- จุดอ่อนที่ขัดขวางความสำเร็จ
- แนวทางการพัฒนาตนเอง
        `;
      } else if (category === "purpose") {
        prompt = `
คุณคือนักปรัชญาผู้เชี่ยวชาญด้านจุดมุ่งหมายชีวิต
ชื่อ: ${name}, วันเกิด: ${birthdate}
คำตอบเชิงลึก:
${custom}

กรุณาวิเคราะห์:
- จุดมุ่งหมายชีวิตที่แท้จริง
- พันธกิจส่วนตัว
- ค่านิยมหลัก
- เป้าหมายระยะสั้น-ยาว
- อุปสรรคที่ต้องระวัง
- แผนชีวิตที่จับต้องได้
        `;
      } else if (category === "relationship") {
        prompt = `
คุณคือผู้เชี่ยวชาญด้านความสัมพันธ์และพลังงานชีวิต
ชื่อ: ${name}, วันเกิด: ${birthdate}

ข้อมูลเชิงประเมิน:
${custom}

กรุณาวิเคราะห์:
- ความสัมพันธ์ที่เหมาะสม
- ประเภทคนที่ควรอยู่ใกล้/เลี่ยง
- วิธีดูแลพลังงานและขอบเขต
- การจัดการความขัดแย้ง
        `;
      } else if (category === "career") {
        prompt = `
คุณคือที่ปรึกษาด้านอาชีพและการเงิน
ชื่อ: ${name}, วันเกิด: ${birthdate}
คำตอบลูกค้า:
${custom}

กรุณาวิเคราะห์:
- อาชีพเหมาะสม
- จุดแข็งสร้างรายได้
- กลยุทธ์การเงิน
- แผนพัฒนาทักษะ
- วิธีบาลานซ์งาน-ชีวิต
        `;
      } else if (category === "bonus") {
        prompt = `
คุณคือผู้เชี่ยวชาญด้านสุขภาพแบบองค์รวมและการพัฒนาจิตวิญญาณ
ข้อมูล:
- ชื่อ: ${name}, วันเกิด: ${birthdate}
- เวลาเกิด: ${birthtime}, สถานที่เกิด: ${birthplace}

คำตอบลูกค้า:
${custom}

กรุณาให้คำแนะนำเรื่อง:
- สมดุลสุขภาพ
- การจัดการความเครียด
- การฝึกสติ
- การเชื่อมโยงกับตัวเองในระดับลึก
        `;
      }

      const encodedPrompt = encodeURIComponent(prompt);
      const finalLink = `https://chat.openai.com/?prompt=${encodedPrompt}`;
      window.open(finalLink, "_blank");
    }
  </script>
</body>
</html>
